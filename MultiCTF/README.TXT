This is an old attempt at making XMP by converting CTF into it. Essentially that makes for CTF with multiple flags.

Most likely doesn't compile any more, but it's here for reference. Might still be useful at some point.